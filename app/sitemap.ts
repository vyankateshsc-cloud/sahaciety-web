import { MetadataRoute } from "next";
import { knowledge, serviceCategories, services, solutions } from "../data/site";

export default function sitemap(): MetadataRoute.Sitemap { const base = "https://www.sahaciety.in"; const topPriority = ["", "/diagnostic"]; return ["", "/diagnostic", "/about", "/services", "/solutions", "/partners", "/become-a-partner", "/knowledge", "/ask-sahaciety", "/contact", "/login"].map((path) => ({ url: `${base}${path}`, lastModified: new Date(), priority: topPriority.includes(path) ? 1 : 0.7 })).concat(serviceCategories.map(({ slug }) => ({ url: `${base}/services/${slug}`, lastModified: new Date(), priority: 0.6 })), services.map(({ slug }) => ({ url: `${base}/service/${slug}`, lastModified: new Date(), priority: 0.5 })), solutions.map(({ slug }) => ({ url: `${base}/solutions/${slug}`, lastModified: new Date(), priority: 0.6 })), knowledge.map(({ slug }) => ({ url: `${base}/knowledge/${slug}`, lastModified: new Date(), priority: 0.5 }))); }