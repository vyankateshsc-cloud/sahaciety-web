import Link from "next/link";
import { ArrowRight, ArrowUpRight, Check, ChevronRight, CircleHelp, Handshake, ShieldCheck, Sparkles } from "lucide-react";
import IntentSearch from "./IntentSearch";
import { Footer, Header, SectionHeading } from "./Shell";
import { ecosystem, journeys, knowledge, serviceCategories, trustPrinciples } from "../data/site";

export default function Site() {
  return <><Header /><main>
    <section className="hero"><div className="hero-glow" /><div className="container hero-grid"><div className="hero-copy"><p className="eyebrow eyebrow-light"><ShieldCheck size={15} /> Governance-led. Service-enabled. Participation-driven.</p><h1>One ecosystem for people, business &amp; society.</h1><p className="hero-lede">Sahaciety brings services, finance, compliance, property, community and governance into one trusted ecosystem - helping you discover what you need, get it done, and participate in what comes next.</p><div className="hero-actions"><Link href="/services" className="button button-primary">Explore services <ArrowUpRight size={17} /></Link><Link href="/ask-sahaciety" className="button button-ghost">Tell us what you need <ArrowRight size={17} /></Link></div><IntentSearch /></div><div className="hero-model"><div className="model-top"><span>One operating model</span><Sparkles size={18} /></div><div className="model-track"><div><b>01</b><span>People &amp; relationships</span></div><div><b>02</b><span>Service &amp; request</span></div><div><b>03</b><span>Governance &amp; execution</span></div><div><b>04</b><span>Participation &amp; benefits</span></div></div><p>The public experience stays simple while the underlying runtime keeps the journey accountable.</p></div></div></section>
    <section className="trust-strip"><div className="container trust-items">{trustPrinciples.map(([title, summary]) => <div key={title}><strong>{title}</strong><span>{summary}</span></div>)}</div></section>
    <section className="section" id="ecosystem"><div className="container"><SectionHeading eyebrow="Start with your context" title="Built around people and institutions - not isolated services." intro="You do not need to understand the whole system before you begin. Start with who you are or what you need, and find the right path." /><div className="ecosystem-grid">{ecosystem.map(({ title, summary, icon: Icon, href }) => <Link href={href} className="ecosystem-card" key={title}><span className="icon-tile"><Icon size={23} /></span><h3>{title}</h3><p>{summary}</p><span className="card-link">Explore path <ArrowUpRight size={16} /></span></Link>)}</div></div></section>
    <section className="section section-soft" id="services"><div className="container"><SectionHeading eyebrow="What you can do" title="A service universe with a clear starting point." intro="Explore by goal, then understand the relevant service, people, documents and next step." /><div className="category-grid">{serviceCategories.slice(0, 6).map(({ title, summary, icon: Icon, slug, services }) => <Link href={`/services/${slug}`} className="category-card" key={slug}><div className="category-icon"><Icon size={22} /></div><h3>{title}</h3><p>{summary}</p><span>{services.slice(0, 3).join(" / ")} <ChevronRight size={16} /></span></Link>)}</div><div className="center-action"><Link href="/services" className="text-link">View all services <ArrowRight size={17} /></Link></div></div></section>
    <section className="section" id="how-it-works"><div className="container"><SectionHeading eyebrow="How it works" title="From need to outcome, with governance built in." intro="Sahaciety helps a simple public journey connect to accountable execution behind the scenes." align="center" /><div className="journey-line">{journeys.map(([title, summary], index) => <div className="journey-step" key={title}><span>0{index + 1}</span><h3>{title}</h3><p>{summary}</p></div>)}</div><div className="workflow-band"><div><p className="eyebrow eyebrow-light">Operational trust</p><h3>From request to completion.</h3><p>You should know what happens next. Sahaciety makes the service journey easier to understand while the Runtime provides the records, responsibilities and timelines underneath.</p></div><div className="workflow-steps">{["Request", "Workflow", "Task", "Evidence", "Completion"].map((step, index) => <div key={step}><span>{String(index + 1).padStart(2, "0")}</span>{step}</div>)}</div></div></div></section>
    <section className="section participation"><div className="container split-section"><div><SectionHeading eyebrow="Participation" title="Get a service today. Become part of the ecosystem tomorrow." intro="A successful service journey can open a wider relationship with people, partners and communities - without making promises before they are defined." /><Link href="/partners" className="button button-dark">Explore participation <ArrowUpRight size={17} /></Link></div><div className="participation-grid">{[["Members", "Access services and participate in the ecosystem.", CircleHelp], ["Partners", "Bring expertise, services and local capacity.", Handshake], ["Centres", "Extend trusted assistance into communities.", ShieldCheck], ["Benefits", "Discover value that grows from responsible participation.", Check]].map(([title, summary, Icon]) => <div className="participation-card" key={title as string}><Icon className="participation-icon" size={23} /><h3>{title as string}</h3><p>{summary as string}</p></div>)}</div></div></section>
    <section className="section knowledge-preview" id="knowledge"><div className="container split-section"><div><SectionHeading eyebrow="Knowledge centre" title="Understand before you act." intro="Guides, explainers and checklists that turn a broad question into a better next step." /><Link href="/knowledge" className="text-link">Browse the knowledge centre <ArrowRight size={17} /></Link></div><div className="knowledge-list">{knowledge.slice(0, 4).map((article) => <Link href={`/knowledge/${article.slug}`} key={article.slug}><span>{article.category}</span><strong>{article.title}</strong><ArrowUpRight size={17} /></Link>)}</div></div></section>
    <section className="ask-band"><div className="container ask-inner"><div><p className="eyebrow">Not sure where to start?</p><h2>Tell Sahaciety what you are trying to accomplish.</h2></div><Link href="/ask-sahaciety" className="button button-primary">Ask Sahaciety <ArrowUpRight size={17} /></Link></div></section>
  </main><Footer /></>;
}
